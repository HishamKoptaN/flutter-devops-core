name: setup-shorebird
description: setup-shorebird
runs:
  using: "composite"
  steps:
    - uses: actions/cache@v4
      with:
        path: |
         /home/runner/.config/shorebird
         /home/runner/.pub-cache
        key: ${{ runner.os }}-${{ hashFiles('**/pubspec.lock') }}
    - uses: shorebirdtech/setup-shorebird@v1
      with:
        cache: true

        