name: android-signing
description: android-signing
inputs:
  flavor:
   required: true
runs:
  using: "composite"
  steps:
    - shell: bash
      run: |
        sed -i 's|^storeFile=.*|storeFile=${{ inputs.flavor }}/keyStore.jks|' android/key.properties