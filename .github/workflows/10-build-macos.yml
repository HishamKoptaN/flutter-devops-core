name: Build macOS

on:
  workflow_call:
    inputs:
      flavor:
        type: string
        required: true

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/cache@v4
        with:
          path: ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - run: flutter config --enable-macos-desktop

      - run: flutter pub get

      - run: flutter build macos --release --dart-define=ENV=${{ inputs.flavor }}

      - uses: actions/upload-artifact@v4
        with:
          name: macos-app-${{ inputs.flavor }}
          path: build/macos/**/*.app
          retention-days: 1
