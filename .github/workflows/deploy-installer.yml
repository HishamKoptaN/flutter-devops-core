name: Build Installer
on:
  workflow_call:
    inputs:
      flavor:
        type: string
        required: true
      full_version:
        type: string
        required: true
      exe_name:
        type: string
        required: false
        default: "app.exe"
      app_name:
        type: string
        required: false
        default: "MyApp"
      app_publisher:
        type: string
        required: false
        default: "MyCompany"
    outputs:
      installer_name:
        value: ${{ jobs.build.outputs.installer_name }}
jobs:
  build:
    runs-on: windows-latest
    environment: ${{ inputs.flavor }}
    outputs:
      installer_name: ${{ steps.assets.outputs.installer_name }}
    steps:
      - shell: pwsh
        id: check_windows
        run: |
          if (Test-Path "windows" -PathType Container) {
            Write-Host "✅ Windows project found" -ForegroundColor Green
            echo "has_windows=true" >> $env:GITHUB_OUTPUT
          } else {
            Write-Host "⚠️ No Windows project found, skipping Windows build" -ForegroundColor Yellow
            echo "has_windows=false" >> $env:GITHUB_OUTPUT
          }

      - uses: actions/checkout@v4
        if: steps.check_windows.outputs.has_windows == 'true'
        with:
          fetch-depth: 0

      - shell: pwsh
        if: steps.check_windows.outputs.has_windows == 'true'
        run: |
          Write-Host "Current directory: $(Get-Location)"
          Write-Host "Listing files:"
          Get-ChildItem -Recurse -Filter "*.iss" | ForEach-Object { Write-Host $_.FullName }

      - run: git config --system core.longpaths true
        if: steps.check_windows.outputs.has_windows == 'true'

      - uses: actions/cache@v4
        if: steps.check_windows.outputs.has_windows == 'true'
        with:
          path: ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - uses: subosito/flutter-action@v2
        if: steps.check_windows.outputs.has_windows == 'true'
        with:
          channel: stable
          cache: true

      - uses: actions/download-artifact@v4
        if: steps.check_windows.outputs.has_windows == 'true'
        with:
          name: windows-exe-${{ inputs.flavor }}
          path: build/windows/x64/runner/Release/
        continue-on-error: true

      - shell: pwsh
        if: steps.check_windows.outputs.has_windows == 'true'
        run: |
          # Check if we have pre-built artifact
          $exePath = "build/windows/x64/runner/Release/*.exe"
          if (Test-Path $exePath) {
            Write-Host "✅ Using pre-built EXE from artifact" -ForegroundColor Green
          } else {
            Write-Host "🔨 Building Windows app..." -ForegroundColor Yellow
            flutter build windows --release --dart-define=ENV=${{ inputs.flavor }}
            if ($LASTEXITCODE -ne 0) { exit 1 }
            Write-Host "✅ Windows app built" -ForegroundColor Green
          }

      - shell: pwsh
        if: steps.check_windows.outputs.has_windows == 'true'
        run: |
          $issPath = "installers\setup.iss"
          Write-Host "Looking for ISS file at: $issPath"
          if (Test-Path $issPath) {
            $content = Get-Content $issPath -Raw
            $content = $content -replace "APP_VERSION_PLACEHOLDER", "${{ inputs.full_version }}"
            $content = $content -replace "APP_NAME_PLACEHOLDER", "${{ inputs.app_name }}"
            $content = $content -replace "APP_PUBLISHER_PLACEHOLDER", "${{ inputs.app_publisher }}"
            $content = $content -replace "APP_EXE_PLACEHOLDER", "${{ inputs.exe_name }}"
            $content = $content -replace "APP_GUID_PLACEHOLDER", "$(New-Guid)"
            $content = $content -replace "APP_SUFFIX_PLACEHOLDER", "${{ inputs.flavor == 'dev' && ' Dev' || '' }}"
            [System.IO.File]::WriteAllText($issPath, $content)
            Write-Host "✅ Template prepared" -ForegroundColor Green
          } else {
            Write-Host "❌ ISS file not found at $issPath" -ForegroundColor Red
            Write-Host "Current directory: $(Get-Location)"
            Get-ChildItem -Recurse | ForEach-Object { Write-Host $_.FullName }
            exit 1
          }

      - shell: pwsh
        if: steps.check_windows.outputs.has_windows == 'true'
        run: |
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "installers\setup.iss"
          if ($LASTEXITCODE -ne 0) {
            Write-Host "❌ Inno Setup failed with exit code $LASTEXITCODE" -ForegroundColor Red
            exit 1
          }
          Write-Host "✅ Installer built" -ForegroundColor Green

      - shell: pwsh
        id: assets
        if: steps.check_windows.outputs.has_windows == 'true'
        run: |
          $flavor = "${{ inputs.flavor }}"
          $appName = "${{ inputs.app_name }}"
          $suffix = if ($flavor -eq "dev") { " Dev" } else { "" }
          $name = "$appName$suffix`_setup.exe"
          echo "installer_name=$name" >> $env:GITHUB_OUTPUT
          Write-Host "Installer name: $name"
        # Add a fallback step for projects without Windows support
      - shell: pwsh
        if: steps.check_windows.outputs.has_windows != 'true'
        id: assets_fallback
        run: |
          Write-Host "⚠️ No Windows build - skipping installer" -ForegroundColor Yellow
          echo "installer_name=none" >> $env:GITHUB_OUTPUT

      - uses: actions/upload-artifact@v4
        if: steps.check_windows.outputs.has_windows == 'true'
        with:
          name: installer
          path: ./Output/*_setup.exe
          retention-days: 5
